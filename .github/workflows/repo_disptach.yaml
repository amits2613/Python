name: server_restart
on:
  repository_dispatch:
    types: [trigger-repo]

jobs:
  repo_dispatch_trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Set branch name
        id: vars
        run: |
          echo result=${{ toJson(github.event.client_payload.branch_name) }}
          branch=$result
          echo $branch
          # $echo "BRANCH=$branch" >> $GITHUB_OUTPUT

      # - name: get branch name
      #   run: echo ${{ steps.vars.outputs.BRANCH }}
